{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch current file",
            "type": "bun",
            "request": "launch",
            "program": "${file}"
        }
    ]
}
